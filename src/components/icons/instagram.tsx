import React from 'react';
import Ig from '@emotion-icons/boxicons-logos/Instagram'
import styled from '@emotion/styled'

const InstagramIcon = styled(Ig)`
  color: red;
`

export const Instagram: React.FC = () => (
    <InstagramIcon/>
);